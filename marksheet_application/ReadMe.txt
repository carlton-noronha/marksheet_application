/*
            Author of code: Carlton Noronha
*/

How to make this application work:

1. Run: npm install
2. Run: npm start
3. Run: npm install -g json-server (Only if you don't have a json-server)
4. Run: json-server --watch data/db.json --port 9999 --id _id